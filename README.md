# DSA--Doubly-Linded-List-
A menu-driven C++ program implementing a doubly linked list with dynamic memory allocation. It supports insertion at a specified index, deletion by value, appending, and extending the list. The program maintains `head`, `tail`, and node count, while enabling efficient forward and backward traversal using `next` and `prev` pointers.
